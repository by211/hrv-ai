# Copy this file to keystore.properties after creating the upload keystore.
# keystore.properties and keystore files are intentionally ignored by source control.
storeFile=/absolute/path/to/hrv-upload.jks
storePassword=REPLACE_WITH_UPLOAD_STORE_PASSWORD
keyAlias=hrv-upload
keyPassword=REPLACE_WITH_UPLOAD_KEY_PASSWORD
